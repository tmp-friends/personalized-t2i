[project]
name = "pigreward-repro"
version = "0.1.0"
description = "Independent conservative adapter for the public PIGReward evaluator"
requires-python = ">=3.12,<3.13"
dependencies = []
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
[tool.hatch.build.targets.wheel]
packages = ["src/pigreward_repro"]
